// ─────────────────────────────────────────────────────────────────────────────
// gap.ts — تایپ‌های فاصله غیرفعالی (TASK-007)
// ─────────────────────────────────────────────────────────────────────────────

/**
 * فاصله غیرفعالی که کاربر باید درباره‌اش توضیح بدهد.
 * از Server Component به GapForm پاس می‌شود — تاریخ‌ها از پیش فرمت شده‌اند.
 */
export interface PendingGap {
  /** نمایش شمسی روز اول فاصله */
  fromDateLabel: string;
  /** نمایش شمسی روز آخر فاصله */
  toDateLabel: string;
  /** تعداد روزهای فاصله (۱ یا بیشتر) */
  days: number;
}

// ─────────────────────────────────────────────────────────────────────────────
// ورودی/خروجی API
// ─────────────────────────────────────────────────────────────────────────────

export interface CreateGapInput {
  note?: string;
}

export interface CreateGapResult {
  ok: boolean;
  error?: string;
  message?: string;
}
