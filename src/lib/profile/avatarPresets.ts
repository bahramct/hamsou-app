// ─────────────────────────────────────────────────────────────────────────────
// رنگ آواتار — تک‌رنگِ ثابتِ برند (DECISION-057)
//
// تاریخچه: در DECISION-056 پالت ۱۲ رنگی معرفی شد. اکنون (DECISION-057) پالت حذف
// شده و آواتارِ بدون‌عکس همیشه رنگِ طلاییِ دیزاین‌سیستم («gold» #C19A4A) را دارد.
// اگر کاربر عکس بگذارد عکس نمایش داده می‌شود؛ اگر عکس را پاک کند دوباره همین رنگ.
//
// نکته: ستونِ `avatarPreset` در schema حفظ شده (حذف ستون = migration پرریسک) اما
// دیگر در UI انتخاب نمی‌شود و در نمایش نادیده گرفته می‌شود. مقدار پیش‌فرضِ ۷
// (ایندکس gold) صرفاً برای سازگاری داده است.
// ─────────────────────────────────────────────────────────────────────────────

/** رنگِ ثابتِ آواتار — طلاییِ برند (توکن `gold`). */
export const AVATAR_COLOR = { bg: "#C19A4A", fg: "#1A1A1F" } as const;

/** رنگِ آواتار را برمی‌گرداند — همیشه طلاییِ ثابت (DECISION-057). */
export function getAvatarColor() {
  return AVATAR_COLOR;
}
